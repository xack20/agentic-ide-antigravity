plugins {
    id 'org.springframework.boot'
}

dependencies {
    implementation project(':cart:infrastructure')
    implementation 'org.springframework.boot:spring-boot-starter-web'
}

bootJar {
    archiveBaseName = 'cart-command-handler'
}
