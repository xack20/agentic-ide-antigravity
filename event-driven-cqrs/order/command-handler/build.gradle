plugins {
    id 'org.springframework.boot'
}

dependencies {
    implementation project(':order:infrastructure')
    implementation 'org.springframework.boot:spring-boot-starter-web'
}

bootJar {
    archiveBaseName = 'order-command-handler'
}
